# dashboards

Dashboards as code: Grafana dashboard JSON shipped as ConfigMaps carrying the sidecar label (`grafana_dashboard: "1"`), which the Grafana sidecar loads. Provisioning config lives in `../grafana/`.
