# Go
bin/
*.exe
*.test
*.out
vendor/

# Node / TypeScript
node_modules/
dist/
.next/
*.tsbuildinfo

# Python
__pycache__/
*.pyc
.venv/
venv/

# Rust
target/

# Env / secrets
.env
.env.*
!.env.example

# Build / generated
# sdk/go/gen is committed on purpose; `task proto:check` fails on drift.
coverage.out
coverage.html

# air live-reload builds (`task dev`)
/tmp/

# Local TLS material from `task certs:dev`. On a cluster, cert-manager issues it.
/certs/

# act: secrets never leave the machine; env.env is committed empty on purpose.
.act/secrets.env

# IDE / OS: shared VS Code config is committed, personal settings are not
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
