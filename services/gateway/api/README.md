# api

The gateway's HTTPS API (`/healthz`, `/v1/route`) is documented in this service's [README](../README.md). It forwards to `services/runtime`'s gRPC contract in [`proto/neuromesh/v1/runtime.proto`](../../../proto/neuromesh/v1/runtime.proto).
